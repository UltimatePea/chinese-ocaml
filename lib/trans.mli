val transpile : string -> string
(** [transpile src] 将骆言源码转换为标准OCaml源码。

    语法规则：
    - 标识符用角括号括起：「名称」→ 名称
    - 注释用角括号加冒号：「：内容：」→ (* 内容 *)
    - 全角括号：（）→ ()，【】→ []
    - Unicode箭头：→ → ->，← → <-，≤ → <=，≥ → >=，≠ → <>
    - 句号：。→ ;;
    - 中文关键字：让→let，递归→rec，在→in，如果→if，等等
*)
