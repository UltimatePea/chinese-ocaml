「：骆言示例程序：排序算法集合：」

「：=== 简单插入排序 ===：」
递归夫「插入排序」者受 列表 焉算法乃
  观「列表」之性
  若 空空如也 则 答 空空如也
  若 有首有尾 首名为「头部;」 尾名为「尾部」 则
    设「排序尾部」为插入排序 尾部
    答 插入元素 头部 排序尾部
  观毕
是谓

递归夫「插入元素」者受 元素 有序列表 焉算法乃
  观「有序列表」之性
  若 空空如也 则 答 有首有尾 首名为「元素;」 尾名为「空空如也」
  若 有首有尾 首名为「头部;」 尾名为「尾部」 则
    若 元素 <= 头部 则
      答 有首有尾 首名为「元素;」 尾名为「有序列表」
    余者
      设「插入尾部」为插入元素 元素 尾部
      答 有首有尾 首名为「头部;」 尾名为「插入尾部」
  观毕
是谓

「：=== 快速排序算法 ===：」
递归夫「快速排序」者受 列表 焉算法乃
  观「列表」之性
  若 空空如也 则 答 空空如也
  若 有首有尾 首名为「基准;」 尾名为「尾部」 则
    设「小于基准」为筛选小于 尾部 基准
    设「大于基准」为筛选大于 尾部 基准
    设「等于基准」为筛选等于 列表 基准
    
    设「左半部」为快速排序 小于基准
    设「右半部」为快速排序 大于基准
    
    答 连接列表 左半部 得 连接列表 等于基准 右半部
  观毕
是谓

「：=== 辅助函数：筛选操作 ===：」
递归夫「筛选小于」者受 列表 值 焉算法乃
  观「列表」之性
  若 空空如也 则 答 空空如也
  若 有首有尾 首名为「头部;」 尾名为「尾部」 则
    若 头部 < 值 则
      设「筛选尾部」为筛选小于 尾部 值
      答 有首有尾 首名为「头部;」 尾名为「筛选尾部」
    余者
      答 筛选小于 尾部 值
  观毕
是谓

递归夫「筛选大于」者受 列表 值 焉算法乃
  观「列表」之性
  若 空空如也 则 答 空空如也
  若 有首有尾 首名为「头部;」 尾名为「尾部」 则
    若 头部 > 值 则
      设「筛选尾部」为筛选大于 尾部 值
      答 有首有尾 首名为「头部;」 尾名为「筛选尾部」
    余者
      答 筛选大于 尾部 值
  观毕
是谓

递归夫「筛选等于」者受 列表 值 焉算法乃
  观「列表」之性
  若 空空如也 则 答 空空如也
  若 有首有尾 首名为「头部;」 尾名为「尾部」 则
    若 头部 == 值 则
      设「筛选尾部」为筛选等于 尾部 值
      答 有首有尾 首名为「头部;」 尾名为「筛选尾部」
    余者
      答 筛选等于 尾部 值
  观毕
是谓

「：=== 辅助函数：列表连接 ===：」
递归夫「连接列表」者受 列表1 列表2 焉算法乃
  观「列表1」之性
  若 空空如也 则 答 列表2
  若 有首有尾 首名为「头部;」 尾名为「尾部」 则
    设「连接尾部」为连接列表 尾部 列表2
    答 有首有尾 首名为「头部;」 尾名为「连接尾部」
  观毕
是谓

「：=== AI友好语法示例：冒泡排序 ===：」
让 「冒泡排序」 = 函数 「列表」 ->
  让 rec 「单次冒泡」 = 函数 「lst」 ->
    观「lst」之性
    若 空空如也 则 答 空空如也  
    若 有首有尾 首名为「x;」 尾名为「空空如也」 则 答 lst
    若 有首有尾 首名为「x;」 尾名为「有首有尾 首名为「y;」 尾名为「余下」」 则
      如果 x > y 那么
        设「冒泡余下」为单次冒泡 得 有首有尾 首名为「x;」 尾名为「余下」
        答 有首有尾 首名为「y;」 尾名为「冒泡余下」
      否则
        设「冒泡余下」为单次冒泡 得 有首有尾 首名为「y;」 尾名为「余下」
        答 有首有尾 首名为「x;」 尾名为「冒泡余下」
    观毕
  在
  让 rec 「重复冒泡」 = 函数 「lst」「次数」 ->
    如果 次数 <= 0 那么
      lst
    否则
      设「一次结果」为 单次冒泡 lst
      重复冒泡 一次结果 (次数 - 1)
  在
  重复冒泡 列表 10

「：=== 测试和演示 ===：」
设「测试数据」为(列开始 64 其一 34 其二 25 其三 12 其一 22 其二 11 其三 90 其一 88 其二 列结束)

打印 『===排序算法演示===』
打印 『原始数据：』
打印 测试数据

打印 『1. 插入排序结果：』
设「插入结果」为插入排序 测试数据
打印 插入结果

打印 『2. 快速排序结果：』
设「快速结果」为快速排序 测试数据  
打印 快速结果

打印 『3. 冒泡排序结果：』
设「冒泡结果」为冒泡排序 测试数据
打印 冒泡结果

「：验证基本功能：」
设「简单测试」为(列开始 3 其一 1 其二 4 其三 1 其一 5 其二 列结束)
打印 『简单测试数据：』
打印 简单测试
打印 『插入排序结果：』
打印 得 插入排序 简单测试

打印 『排序算法演示完成！』