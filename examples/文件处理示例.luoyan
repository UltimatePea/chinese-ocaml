(* 骆言文件处理示例 - 展示文件I/O功能 *)

让 演示文件操作 = 函数 文件名 ->
  让 消息1 = 打印 "=== 骆言文件处理演示 ===" 在
  让 消息2 = 打印 (字符串连接 "检查文件是否存在: " 文件名) 在
  
  让 存在检查 = 
    如果 文件存在 文件名 那么
      打印 "文件存在！"
    否则
      打印 "文件不存在，将创建新文件"
  在
  
  (* 写入示例内容 *)
  让 示例内容 = "这是用骆言编程语言创建的文件！\\n编译器: 骆言自举编译器\\n日期: 2025年7月12日\\n功能: 文件I/O操作演示\\n\\n骆言是世界首个自举的中文编程语言！" 在
  
  让 写入结果 = 写入文件 文件名 示例内容 在
  让 消息3 = 打印 (字符串连接 "成功写入文件: " 文件名) 在
  
  (* 读取并显示内容 *)
  让 文件内容 = 读取文件 文件名 在
  让 消息4 = 打印 "文件内容如下:" 在
  让 消息5 = 打印 文件内容 在
  
  打印 "文件操作演示完成！"

(* 主程序 *)
让 演示文件 = "demo_output.txt"
演示文件操作 演示文件