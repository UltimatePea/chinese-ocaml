/* 骆言包管理系统 - CLI工具集成测试 */
/* Author: Whisky, PR Worker */
/* 验证CLI工具的用户体验和功能完整性 */

/* 测试包管理基本功能 */
吾有一言。曰「骆言包管理CLI测试」。名之曰「测试标题」。
書之。

/* 测试包安装功能 */
吾有一言。曰「测试包」。名之曰「包名」。
安装包「包名」。

/* 测试包列表功能 */
列出已安装包。

/* 测试包搜索功能 */
吾有一言。曰「数学」。名之曰「搜索词」。
搜索包「搜索词」。

/* 测试包信息查询 */
包信息「包名」。

/* 测试包卸载功能 */
卸载包「包名」。

/* 测试项目初始化 */
吾有一言。曰「测试项目」。名之曰「项目名」。
初始化项目「项目名」。

/* 测试配置文件操作 */
读取包配置。

/* 测试缓存管理 */
清理缓存。
重建缓存。
缓存状态。

/* 输出测试完成信息 */
吾有一言。曰「✅ CLI集成测试完成」。名之曰「完成消息」。
書之「完成消息」。