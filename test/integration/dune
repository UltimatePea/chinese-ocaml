;; 骆言编译器集成测试配置
;; Issue #1357 Token系统兼容性集成测试

; Temporarily disabled due to token system changes
; (test
;  (name test_token_compatibility_integration)
;  (modules test_token_compatibility_integration)
;  (libraries yyocamlc_lib alcotest token_system_unified_core token_system_unified_conversion)
;  (preprocess
;   (pps bisect_ppx)))

;; Issue #1709 统一模块质量验证测试

(test
 (name test_unified_modules_quality_validation)
 (modules test_unified_modules_quality_validation)
 (libraries yyocamlc_lib alcotest)
 (preprocess
  (pps bisect_ppx)))

;; Issue #1709 数据迁移验证测试

(test
 (name test_data_migration_validation)
 (modules test_data_migration_validation)
 (libraries yyocamlc_lib alcotest)
 (preprocess
  (pps bisect_ppx)))

;; Issue #1746 Poetry模块数据验证测试
;; Temporarily disabled due to Unicode path issues in Alcotest

; (test
;  (name test_poetry_module_data_validation)
;  (modules test_poetry_module_data_validation)
;  (libraries yyocamlc_lib alcotest unix)
;  (preprocess
;   (pps bisect_ppx)))
