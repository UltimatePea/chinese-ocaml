;; 技术债务清理回归测试配置
;; Author: Echo, 测试工程师代理 - Support Issue #1576

;; 主要回归测试套件 - 保护所有Phase的重构过程

(test
 (name test_tech_debt_cleanup_regression)
 (modules test_tech_debt_cleanup_regression)
 (libraries yyocamlc_lib poetry alcotest unix str)
 (flags
  (:standard -w -26-27-32))
 ;; 抑制未使用变量警告
 (preprocess
  (pps bisect_ppx)))

;; Poetry模块整合回归测试 - Fix #1709

(test
 (name test_poetry_integration_regression)
 (modules test_poetry_integration_regression)
 (libraries yyocamlc_lib poetry alcotest)
 (flags
  (:standard -w -26-27-32))
 (preprocess
  (pps bisect_ppx)))

;; Issue #1746 Poetry数据准确性验证测试

(test
 (name test_poetry_data_accuracy_validation)
 (modules test_poetry_data_accuracy_validation)
 (libraries yyocamlc_lib poetry alcotest unix)
 (flags
  (:standard -w -26-27-32))
 (preprocess
  (pps bisect_ppx)))

;; 性能基准测试 - 待实现
;; TODO: 添加 performance_benchmark_suite.ml 文件

;; 内存使用监控 - 待实现  
;; TODO: 添加 memory_usage_monitor.ml 文件
